%h3 Edit Facility

= error_messages_for :facility

= form_for(@facility, :action => resource(@facility)) do
  = partial "form"

  %p= submit "Update"

= link_to 'Show', resource(@facility)
|
= link_to 'Back', resource(:facilities)