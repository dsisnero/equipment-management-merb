%h3 Edit Building

= error_messages_for :building

= form_for(@building, :action => resource(@site,@building)) do
  %fieldset
    = partial "form"   
    %p= submit "Update"

= link_to 'Show', resource(@site,@building)
|
= link_to 'Back', resource(@site,:buildings)