%h2 New Site

= error_messages_for :site

= form_for(@site,:action => resource(:sites) ) do
  %fieldset
    %legend Create Site
    =partial 'form'
  
    %p= submit "Create"

= link_to 'Sites', resource(:sites)